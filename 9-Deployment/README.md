# What is langgraph.json ?

Its a contract between code and langgraph platform
It tells the platform:
- What dependencies to install
- Which graphs are exist
- where the graph objects live
- which environment config to load